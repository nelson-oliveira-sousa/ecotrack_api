#!/usr/bin/env ruby
# bin/mqtt_worker

# Permite que o log apareça em tempo real no console/Docker
$stdout.sync = true
puts "🚀 Iniciando EcoTrack MQTT Worker..."

# Carrega o ambiente do Rails
require_relative "../config/environment"

puts "💎 Rails environment carregado com sucesso."

# Chama o Runner que encapsula toda a lógica
Telemetry::Services::MqttRunner.start
